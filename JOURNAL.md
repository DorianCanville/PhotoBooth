# Journal de bord — PhotoBooth

> Tableau chronologique de toutes les modifications. **Entrée la plus récente en haut.**
> Voir [`CLAUDE.md`](CLAUDE.md) — règle n°1 : ce journal doit être mis à jour après chaque modification de code.

## Légende
- **Risque** : conséquence possible si la modif est mauvaise (aide à diagnostiquer une régression).

---

| Date | Fichiers | Modification | Pourquoi | Risque / à surveiller |
|---|---|---|---|---|
| 2026-06-25 | `frontend capture.component.scss` | **Correctif écart chips Mode/Objectif** : dans `.deck-col`, le `.pb-chip-group` s'étirait sur la largeur de la colonne (`min-width: 280px`) mais les chips ne grandissaient pas → espace vide à droite du groupe. Ajout `.pb-chip-group { width: 100% }` + `.pb-chip { flex: 1 }` scopés à `.deck-col` pour que les chips remplissent le groupe (look segmenté). | Écart visuel à droite des boutons Mode et Objectif. | Scopé à la capture (admin non touché). CSS uniquement. |
| 2026-06-25 | `frontend compositing.service.ts`, `storage.service.ts`, `capture.component.ts/.html/.scss`, `photo-editor.component.ts/.html/.scss` + `backend photos.ts` | **Suppression du système de filtres** : retrait du type `FilterValue`, de `FILTERS`, des signaux `filter`/`reviewFilter`, des `filterCss`/computed, de l'aperçu `[style.filter]`, des chips de filtre (revue + éditeur) et du paramètre `filter` de `composePhoto`/`savePhoto`. Backend : champ `filter` retiré de `metadata.json`. Spéc §5 mise à jour. | Décision commanditaire : filtres non désirés. | Anciennes entrées `metadata.json` gardent un champ `filter` résiduel (ignoré, sans effet). `composePhoto` a une signature changée — vérifier qu'aucun appel externe ne subsiste (grep OK). tsc front+back verts. |
| 2026-06-25 | `frontend styles.scss`, `admin.component.scss`, `editor photo-editor.component.scss` | **Correctifs design (CSS introuvable)** : ajout des classes globales manquantes `.pb-chips` (rangée horizontale → corrige tabs admin & filtres éditeur empilés verticalement), `.pb-btn-accent` et `.pb-btn-ghost` (variantes de bouton jamais définies → boutons sans style accent). Remplacement de 3 variables CSS inexistantes (`--pb-radius`→`--pb-r-md`, `--pb-surface-alt`→`--pb-surface-strong/hover`, `--pb-font`→`--pb-font-ui`) qui rendaient les coins carrés et fonds transparents. | Templates écrits avec des classes/variables jamais déclarées → mise en page cassée (tabs verticaux, coins carrés, bouton × mal placé). | Aucune régression attendue (ajouts de styles + alias de variables). Vérifier sur écran réel. |
| 2026-06-25 | `frontend styles.scss` + `capture.component.html`, `photo-editor.component.html`, `gallery.component.html`, `decoration-picker.component.html`, `official-editor.component.html` | **Icônes emoji → SVG inline** : le Raspberry n'a aucune police emoji couleur (`fc-list` = 0), donc 🖨 🎨 📁 🗑 🚫 s'affichaient en carré tofu (dont « 📁 Importer le **logo** »). Remplacés par des `<svg class="ico">` en `currentColor` (classe `.ico` ajoutée). Les glyphes monochromes rendus correctement (🔒 🔓 ⚙ ✎ ⚠ ✕ ✓) sont conservés. | Icônes invisibles sur le matériel (police emoji absente). | Si une police emoji est installée plus tard, les SVG restent corrects. Boutons concernés : impression, déco, import logo, effacer décos, badge imprimé, déco officielle OFF. |
| 2026-06-25 | `frontend theme.service.ts`, `admin.component.ts/.html` | **Suppression des thèmes inutiles** : ne reste que `studio`. Thèmes `festif` et `minimal` retirés du service, `getThemes()` supprimé, sélecteur de thème retiré de l'admin, `selectedTheme` supprimé (thème codé en dur `studio`). Conforme spéc §7. | Festif/Minimal non utilisés ; la spéc impose Studio unique. | `settings.json` peut encore contenir `theme: "festif"/"minimal"` — `app.ts` retombe sur `studio` (un seul thème défini dans `apply`). À surveiller si d'anciens réglages persistent. |
| 2026-06-25 | `frontend camera.service.ts`, `capture.component.html`, `admin.component.ts` | **Correctif orientation (vraie cause)** : le `<video>` avait DEUX écritures de `transform` en conflit — binding Angular `scaleX(-1)` (miroir) vs `videoEl.style.transform = rotate()` impératif du service. Selon l'ordre de détection de changement, l'un écrasait l'autre → aperçu retourné « par moments ». Rotation transformée en **signal** `rotationDeg`, le template compose `scaleX(-1) rotate(Xdeg)` (source unique). L'admin appelle `setLensRotation` pour appliquer **à chaud** (capture toujours montée derrière la modale). | Le réglage admin d'orientation ne s'appliquait pas à chaud + retournements aléatoires. | Capture : seul 180° géré, photo non mise en miroir (volontaire). Valider sur matériel. |
| 2026-06-25 | `backend/src/config.ts` (nouveau), `backend/src/routes/photos.ts`, `print.ts`, `server.ts` | Centralisation de `PHOTOS_DIR`/`META_FILE`/`PRINTER_NAME` dans un module unique. `print.ts` lisait `backend/../photos` au lieu du vrai dossier → impression réparée (écart §10 #1). | Impression cherchait les fichiers au mauvais endroit. | Vérifier que `PHOTOS_DIR` (ou l'env) pointe bien sur le dossier réel en prod. |
| 2026-06-25 | `backend/src/routes/print.ts`, `frontend storage.service.ts`, `gallery.component.ts/.html` | Impression : choix version décorée/brute (param `variant`, suit l'onglet galerie), copies forcées à 1 (écart §10 #3). | Décision produit. | Si `_raw` absent, retombe sur la version décorée. |
| 2026-06-25 | `backend/src/routes/settings.ts`, `frontend camera.service.ts`, `capture.component.ts`, `admin.component.ts/.html` | Rotation caméra configurable par objectif (0/180°) via réglage admin `lensRotation`, au lieu d'être codée en dur (écart §10 #2/#6). Défauts inchangés (normal=180, wide=0). | Orientation parfois inversée selon l'objectif/montage. | Seul 0/180 est géré en capture (un 90/270 changerait les dimensions du canvas). À valider sur le matériel réel. |
| 2026-06-25 | `SPECIFICATION.md` | Revue fonctionnalité par fonctionnalité avec le commanditaire : décisions actées (objectif visible invité, minuteurs 3/5/10, rafale ×4, zoom admin fixe, déco créée par admin/utilisée par invité, déco officielle affichable au choix de l'invité, suppression admin-only, ré-édition non destructive, impression décorée/brute 1 copie, thème Studio unique, stockage configurable admin, purge admin, PIN changé à la 1re config). Liste d'écarts spéc↔existant en §10. | Cadrer le comportement cible avant implémentation, éviter les dérives. | Documentation. Les écarts §10 ne sont pas encore implémentés — le code actuel diverge de la spéc. |
| 2026-06-25 | `CLAUDE.md`, `JOURNAL.md`, `FONCTIONNALITES.md`, `SPECIFICATION.md` | Création du dispositif de suivi : journal de bord, état des lieux des fonctionnalités, spécification, et règles de travail. | Éviter les régressions répétées et les dérives en encadrant chaque modification. | Aucun (documentation). |

<!--
Modèle d'entrée à copier (à insérer juste sous la ligne d'en-tête du tableau) :

| AAAA-MM-JJ | `chemin/fichier.ts` | Ce qui change concrètement | La raison du changement | Ce qui pourrait casser |
-->
